import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFF1337EC);

  // Light Theme
  static const backgroundLight = Color(0xFFF6F6F8);
  static const textPrimaryLight = Colors.black87;
  static const textSecondaryLight = Colors.black54;
  static const cardLight = Colors.white;
  static const borderLight = Color(0xFFE5E7EB);

  // Dark Theme
  static const backgroundDark = Color(0xFF101322); // home dark
  static const cardDark = Color(0xFF161B22);
  static const textPrimaryDark = Color(0xFFE6EDF3);
  static const textSecondaryDark = Color(0xFF8B949E);
  static const borderDark = Color(0xFF1F2937);

  // Accent
  static const accentOrange = Color(0xFFFFA500);
}
